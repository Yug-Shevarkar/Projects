@model ItemProcessorApp.Models.Item

<li>
    <strong>@Model.Weight</strong>

    @if (Model.Children != null && Model.Children.Count > 0)
    {
        <ul>
            @foreach (var child in Model.Children)
            {
                @Html.Partial("_ItemTree", child)
            }
        </ul>
    }
</li>